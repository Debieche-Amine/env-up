# TOOLS.md

Fabrik uses shell execution, HTTP clients (`curl`), JSON parsers (`jq`), and tunneling utilities (`localtunnel`) to interact directly with Meta Graph APIs and expose local endpoints. Browser automation is disabled.
